name: test
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  testing:
    runs-on: windows-latest
    steps:
    - name: clone repo
      uses: actions/checkout@v2
    - uses: actions/setup-msbuild@v1.1
    - run: msbuild 0.cpp
      
